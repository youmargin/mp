spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mybatis_study?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: root
logging:
  level:
    root: info
    com.app.mp: debug
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false  # 字段采用驼峰方式，不转为下划线
